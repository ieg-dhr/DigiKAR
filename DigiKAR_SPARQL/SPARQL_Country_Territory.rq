SELECT DISTINCT ?item ?itemLabel ?country ?countryLabel ?territory ?territoryLabel
WHERE {
  SERVICE wikibase:label { bd:serviceParam wikibase:language "de". }
  ?item wd: %s.
  OPTIONAL { 
  ?country wdt:P17 ?countryLabel.
  ?territory wdt:P131 ?territoryLabel.
  }
}
